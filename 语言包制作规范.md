# 语见世界语言包制作规范

本文用于新增或重做目的地语言包。目标是把内容质量检查前置，在制作音频以前发现机械句、跨国家词汇、语义错位和重复内容，避免完成后大批返工。

## 1. 当前语言包的标准规模

每个正式语言包应包含：

- 8 个旅行场景。
- 830 条正式内容：510 个词汇、320 条短句。
- 246 条场景化例句。例句附属于重点词汇，不单独计算学习进度。
- 每个场景新增 30 条扩展短句，共 240 条；标准分配为 22 条 `traveler-says` 和 8 条 `traveler-hears`。
- 包内正式内容 ID 唯一，例句 ID 唯一。

八个场景固定为：

| 场景 ID | 中文名称 | 核心内容 |
| --- | --- | --- |
| `airport` | 机场 | 证件、值机、行李、安检、登机、入境 |
| `transport` | 交通 | 车票、公交地铁、铁路、出租车、换乘、租车 |
| `hotel` | 酒店 | 预订、入住、房间设施、需求、问题、退房寄存 |
| `food` | 餐厅 | 等位、菜单、点餐、忌口、用餐需求、确认、结账 |
| `shopping` | 购物 | 找商品、尺码、试用、价格、支付、退换与退税 |
| `directions` | 问路 | 位置、路线、距离、地图地标、听懂与确认 |
| `emergency` | 紧急求助 | 身体不适、看病药店、报警、失物、事故、联络 |
| `basics` | 基础交流 | 问候、礼貌、自我介绍、语言求助、数字、时间、确认 |

上述数量是当前产品契约。若产品以后调整规模，应同时修改构建脚本、音频校验脚本和自动测试，不要只改生成结果。

## 2. 开工前先写“目的地资料卡”

不要先把现有语言包整包机器翻译。应先确认以下资料，并把它们写入生成脚本的人工覆盖表：

- `packId`、目的地 ID、语言代码、`locale`、`speechLocale`。
- 当地货币及常用金额说法。
- 当地紧急号码、报警和急救表达。
- 机场入境制度、常用交通卡、铁路或地铁名称。
- 3 至 5 个适合旅行例句使用的当地城市或地标。
- 当地住宿类型，例如日式旅馆、韩屋、汽车旅馆或西班牙 `parador`。
- 7 个当地代表性食物，用于 `food_017` 至 `food_023`。
- 当地税费、支付、退税、小费和营业习惯。
- 正式与日常场合所需的礼貌等级、性别或敬语差异。
- TTS 声音以及需要特别处理的缩写、数字、外来词和专名。

资料卡未完成前不要批量生成文本或音频。它是防止“韩国包出现纽约”“俄罗斯包出现零美元”“所有国家都串用日本料理”的第一道检查。

## 3. 数据结构要求

语言包保存在 `languages/{packId}/pack.js`，通过 `window.registerContentPack({...})` 注册。顶层至少需要：

```js
{
  id,
  destinationId,
  locale,
  speechLocale,
  languageCode,
  languageLabel,
  nativeLabel,
  pronunciationLabel,
  features,
  scenes,
  entries
}
```

正式内容至少需要：

```js
{
  id,
  sceneId,
  situationId,
  kind,          // "word" 或 "phrase"
  zh,
  text,
  direction,     // "traveler-says" 或 "traveler-hears"
  intent,
  audioPath,
  pronunciation, // 仅在该语言包配置了 pronunciationLabel 时必填
  example: {
    id,
    zh,
    text,
    pronunciation
  }
}
```

规则：

- `sceneId` 和 `situationId` 必须引用真实存在的场景与小情境。
- `kind: "phrase"` 的完整短句不再附加例句。
- 配置了 `pronunciationLabel` 的语言，正式内容和例句均必须有读音字段。
- 不要直接长期维护生成后的数千行 `pack.js`；人工内容应保存在生成脚本、审核目录或覆盖表中，再重新构建。

## 4. 文本编写标准

### 4.1 中文与目标语言必须逐条对齐

- 中文不是模糊标签，而是目标语言内容的准确含义。
- 时段、数量、币种、交通工具、地点和说话人不能错位。
- 例句修改词义后，中文、目标语言、读音和音频必须一起修改。
- 同一个中文词在目标语言中有多个语法形式时，选择旅行场景中最自然的形式，并在例句中验证。

### 4.2 禁止机械扩写

以下形式不能作为批量扩写策略：

- “我想了解‘某词’。”
- “我需要处理‘某词’。”
- “关于‘某词’，下一步该怎么做？”
- “请告诉我‘某词’的情况。”
- 只替换一个名词、其余结构完全相同的一组句子。

每条短句必须对应真实旅行任务，例如询问站台、确认退税条件、说明过敏、要求更换房间或听懂服务人员的回答。

### 4.3 整包去重

交付前必须分别检查：

- 320 条短句的中文完全重复数为 0。
- 320 条短句的目标语言完全重复数为 0。
- 246 条例句的中文完全重复数为 0。
- 246 条例句的目标语言完全重复数为 0。

语义相近不一定错误，但同一句话不应因为属于两个场景而原样出现两次。需要保留相近功能时，应改成更具体的情境表达。

### 4.4 目标国家本地化

- `food_017` 至 `food_023` 必须使用目的地代表性食物，不能复制其他国家的整组词汇。
- 本地货币应出现在核心货币词和日常付款例句中；外币只用于兑换、拒收等合理语境。
- 火车目的地、住宿、交通卡、退税制度和紧急号码必须属于目标国家。
- 不要仅检查目标语言文字，也要检查中文中的地名和币种。
- 地名要形成小而一致的集合，不要在同一目的地包中无理由混用多个国家城市。
- 当地表达优先于字面翻译，例如美国使用 `restroom`、西班牙使用 `IVA`、韩国使用当地交通卡与报警号码。

### 4.5 语体与自然度

- 旅行者说出的句子应简短、礼貌、可直接使用。
- 旅行者听到的句子应像工作人员真实会说的话。
- 同一语言包保持一致的礼貌等级和地区变体。
- 避免翻译腔、界面说明腔、词典释义腔和不完整的名词串。
- 数字、量词、冠词、格变化、敬语和复数形式要在例句中再次检查。

## 5. 高风险内容必须人工审校

机器翻译或批量脚本完成后，以下内容必须逐条人工确认：

1. 报警、救护车、医院、药物、过敏和保险。
2. 货币、金额、找零、信用卡与银行卡。
3. 早上好、下午好、晚上好和晚安等时段表达。
4. 数字在计数、日期、人数和金额中的形式。
5. 入境、签证、海关和申报。
6. 当地城市、铁路、交通卡、住宿及特色食物。
7. 否定句、禁止句以及“可以/不可以”。
8. TTS 容易误读的缩写、拉丁字母、专名和号码。

缓存只用于恢复进度，不能视为审校结果。服务端出现临时 500 时应保存缓存并续跑，完成后仍按最终条目总数检查，不要默认中断点之后已经生成。

## 6. 推荐制作顺序

### 阶段 A：建立骨架

1. 复制共享场景和小情境结构。
2. 配置语言包元数据及目的地资料卡。
3. 建立本语言专用生成脚本和人工覆盖表。
4. 保持 ID 稳定，避免因翻译内容变化而改变学习进度键。

### 阶段 B：先完成全部文本

1. 本地化 510 个词汇。
2. 编写或审核 320 条短句。
3. 编写 246 条词汇例句。
4. 补齐读音字段。
5. 运行整包重复、模板、跨国家词汇和结构检查。
6. 对高风险内容做人工抽查。

文本没有冻结前不要批量制作音频。先改文本、最后统一生成音频，效率最高。

### 阶段 C：制作音频

1. 从最终语言包读取 `text`，不要从旧源文件猜测朗读内容。
2. 使用与 `speechLocale` 一致的地区声音。
3. 文本发生变化的条目必须删除旧 MP3 后重新生成；生成器默认会跳过已有非空文件。
4. 只删除经过清单验证、且位于项目 `audio/` 目录下的明确目标。
5. 网络中断后采用续跑模式，不要删除已成功生成的文件。
6. 完成后检查目标总数、缺失文件、零字节文件和 `.part` 临时文件。
7. 人工试听高风险内容及每种语音至少一组词汇、短句、数字、地名和例句。

注意：`generate_audio.py --rebuild-changed` 中的 1040 项清单是本次五语返工的专用清单，不是未来新语言包的通用模板。新增语言应把语言包、声音和目标路径正式接入生成器，再使用试听与完整模式。

### 阶段 D：产品接入

1. 在 `core/destinations.js` 注册目的地及资源路径。
2. 添加 `languages/{packId}/pack.js`。
3. 如有零基础认读课程，单独添加 `beginner/data.js`、`audio.js`、`module.js` 和教学音频。
4. 更新对应语言包测试及五包通用质量测试。
5. 确认按需加载、收藏、学习进度和紧急联系卡仍按语言包隔离。

## 7. 零基础认读课程制作流程

零基础认读课程与八大旅行场景是两个独立模块。认读课程只负责建立“文字到声音”的能力，不承担词汇、句型或旅行功能教学。

### 7.1 先确定能力边界

结课目标统一为：

1. 认识该语言的基础文字、字母或拼写线索。
2. 掌握最必要的拼读、重音和常见音变。
3. 看着陌生单词时可以尝试读出，而不是依赖中文谐音。
4. 看着简短句子时可以按词组、停顿和自然节奏连续读出。

认读课程不得教授：

- 路牌、菜单或其他旅行标识的含义。
- 点餐、问路、付款、礼貌请求或救场表达的用途。
- 句型功能、语法用途或场景选择。
- “这个词是什么意思”“看到这个词应该去哪里”等词义测试。

单词卡可以保留简短中文释义作为记忆辅助，但释义不进入练习、认读测试或结课判定。短句只作为发音材料，不讲功能和翻译。

### 7.2 按语言难点决定课数

统一结课能力，不强行统一课数。先列出该语言从零开始认读所需的最小能力链，再决定课程数量。当前五语言是可参考的基准，不是新语言必须套用的固定模板：

| 语言 | 课数 | 主要认读负担 |
| --- | ---: | --- |
| 日语 | 18 | 平假名、片假名、浊音、半浊音、拗音、促音、长音及助词特殊读法 |
| 英语 | 12 | 拼写与声音关系、音节、重音、弱读、连读和缩略形式 |
| 韩语 | 15 | 音节方块、基础与复合元音、辅音、收音归并及常见音变 |
| 西班牙语 | 12 | 稳定元音、特殊拼写组合、r/rr、重音和连续认读 |
| 俄语 | 15 | 西里尔字母、软硬变化、重音、元音弱化、辅音变化和连续认读 |

设计时遵循以下顺序：

1. 列出文字系统和基础声音。
2. 列出必须单独讲解的拼读规则。
3. 列出真实读音与拼写不同的高频变化。
4. 安排单词分块和连续认读。
5. 最后安排短句中的停顿、弱读、连读或特殊读法。
6. 将课程归入 5 个阶段；阶段数量统一，阶段内课数可以不同。

如果学习者已经熟悉所用字母，例如英语和西班牙语，不要为了凑课数重复教字母表。新文字系统则必须为识字和结构留出足够课时。

### 7.3 先写课程蓝图，再填内容

课程蓝图至少应列出：

- 5 个阶段的名称、目标和课数。
- 每课唯一 ID、标题、读音能力点及前置知识。
- 每课展示的文字、读音提示、播放内容和即时练习。
- 后续哪一课会再次使用当前学过的规则。
- 12 道认读测试覆盖的能力类别。
- 结课挑战中的 4 个单词和 2 个短句。

蓝图审查通过后再写完整数据。不要先复制某个语言的 12 课模板，再硬塞本语言的文字系统。

选例规则：

- 基础文字课的例词必须真实包含本课所教文字。
- 新元音、收音、重音或音变必须在后续单词中再次出现。
- 例词优先短、清楚、便于听辨；中文释义只作卡片辅助。
- 句子应语义中性、长度短，只用于连读和节奏训练。
- 转写只作为过渡扶手，完整单词应标实际听感，不能把字母名提示当作整词读音。
- 教学重音符号、分音点或分块符号必须明确说明是学习提示，不代表真实书写一定出现。

### 7.4 数据与模块结构

每种语言的认读课程放在 `languages/{packId}/beginner/`：

```text
beginner/
├─ data.js       # 五阶段课程、12 道测试、6 项挑战
├─ audio.js      # 教学音频清单；日语当前使用 audio.json
└─ module.js     # 进度、路由、渲染与交互
```

课程数据统一使用五阶段结构：

```js
{
  stages: [
    {
      id,
      title,
      subtitle,
      lessons: [
        {
          id,
          title,
          subtitle,
          // 本语言需要的教学卡、文字项、例词和即时练习
        }
      ]
    }
  ],
  placement: [/* 12 道认读题 */],
  challenge: [/* 4 个单词 + 2 个短句 */]
}
```

日语等需要专用卡片的语言可以保留 `rows`、`rules` 等辅助数据，但课程总览和进度必须以 `stages[].lessons[]` 为唯一课程顺序来源。

模块实现要求：

- 从课程数组动态计算总课数、当前序号、最后一课和进度条上限。
- 禁止写死 `/ 12`、`index === 11` 或固定末课 ID。
- 上一项、下一项、最后一课返回和挑战解锁都使用实际课程数组。
- 罗马字或转写默认作为辅助，不应比目标文字更突出；需要时可设计为展开后显示。
- 每课至少有一次“先自己读，再播放核对”的闭环，不使用麦克风评分作为必需条件。

### 7.5 认读测试与结课挑战

认读测试固定为 12 题，用来决定学习路线，不与课程数量绑定。题目只能检查：

- 字形与声音对应。
- 拼读规则。
- 音节、重音或长短音。
- 常见音变和真实听感。
- 单词或短句的连续认读。

通过条件为至少答对 10/12，且每个能力类别至少答对一题。类别数量可以按语言特点设置，但所有关键能力都必须覆盖。

结课挑战固定为 6 项，并按以下顺序组织：

1. 前 4 项为单个单词。
2. 后 2 项为简短句子。
3. 用户先自行认读，再播放标准音频核对。
4. 不要求翻译，不按词义判定，不使用场景用途题。
5. 可以把未读熟项目加入 `retryWords`，供再次挑战。

### 7.6 课程版本与进度迁移

每套认读课程进度必须带课程版本：

```js
{
  curriculumVersion: 2,
  completedLessons: [],
  challengeDone: false,
  placementPassed: false,
  retryWords: []
}
```

当课程结构、课 ID、测试或挑战发生不兼容调整时，提高 `curriculumVersion`。首次读取缺少当前版本或版本不一致的旧进度时：

- 重置已完成课程、认读测试结果、挑战结果和重试项。
- 保留用户已经选择的目的地。
- 保留“零基础 / 有基础 / 稍后学习”的水平选择。
- 当前版本进度恢复时过滤不存在的课 ID 和挑战项，避免脏数据卡住导航。

### 7.7 音频清单制作与生成顺序

课程文本和播放项全部冻结后再处理音频：

1. 收集课程卡、即时练习、认读测试和结课挑战中的全部播放项。
2. 以“文字 + 语速 + 地区声音”为复用条件。三者完全相同才能复用旧文件。
3. 旧正式词库音频可以原路径复用；不要为了清单整齐重复生成相同声音。
4. 修改过文字或语速的内容必须分配新 ID，不能让旧 MP3 与新显示文字错位。
5. 英语、西班牙语完整句保留慢速和自然语速两版；其他语言按各自已确定的教学语速策略执行。
6. 先运行各语言的 `validate-only`，确认数量、ID、文字和语速唯一性。
7. 五套数据和清单全部稳定后，最后统一运行完整生成命令；已有且非空的文件由生成器自动跳过。
8. 生成完成后再次运行 `validate-only`，并检查所有引用文件存在且大小大于 0。

不要在课程内容仍在修改时零散生成音频；这会产生大量未引用文件并增加听审返工。

### 7.8 零基础课程验收顺序

按以下顺序验收，前一步失败时不要进入音频批量生成：

1. **边界检查**：搜索词义题、场景用途、礼貌请求和救场教学，确认没有混入认读模块。
2. **结构检查**：验证 5 阶段、预期课数、12 道测试、6 项挑战以及课 ID 唯一。
3. **内容检查**：逐语言核对文字覆盖、规则复现、地区读音口径、转写和真实听感。
4. **进度检查**：验证旧版重置、新版保存恢复、无效课 ID 过滤。
5. **流程检查**：走通总览、上一项/下一项、最后一课、认读测试、挑战和重试项。
6. **移动端检查**：在手机宽度检查长标题、课程总览、动态进度和按钮，无横向溢出。
7. **清单检查**：运行五种教学音频的 `validate-only`。
8. **音频生成**：最后统一补齐缺失 MP3。
9. **最终验收**：运行完整 Node 测试，并再次验证音频文件存在且非空。

新增或重做认读课程时，至少加入以下自动断言：

- 实际课数与设计值一致，且均为 5 个阶段。
- 课 ID、教学音频 ID 唯一。
- 每套有 12 道测试和 6 项挑战。
- 测试题不包含中文释义或场景用途选择。
- 挑战严格为前 4 个单词、后 2 个短句。
- 所有播放项都有有效音频引用。
- 旧版进度被重置，当前版本进度可保存、恢复并过滤失效项。
- 所有教学 MP3 存在且文件大小大于 0。

## 8. 自动检查要求

通用质量测试位于 `tests/content-quality.test.js`。新增语言包时必须把新 `packId` 加入该测试，并补充该目的地的关键断言。

最低检查项：

- 正式内容、词汇、短句和例句数量正确。
- 正式内容 ID 与例句 ID 唯一。
- 场景和小情境引用有效。
- 需要读音的内容没有缺失读音。
- 短句及例句的中外文均无完全重复。
- 中文不含机械模板标记。
- 当地食物、货币、地名、交通和紧急表达正确。
- 每条内容可以组成四个唯一选项。
- 所有音频目标存在且文件大小大于 0。
- 没有遗留 `.part` 文件。

常用命令：

```powershell
# 运行完整测试
node --test tests\*.test.js

# 验证五个正式语言包结构
python -X utf8 scripts\generate_audio.py --validate-only

# 先生成少量正式内容试听
python -X utf8 scripts\generate_audio.py --test

# 文本最终确认后生成完整正式内容与例句音频
python -X utf8 scripts\generate_audio.py --full
```

如果新语言有独立生成脚本，还应运行该脚本的语法检查，并验证重新生成两次得到的结构一致。

## 9. 人工验收清单

在宣布完成前逐项确认：

- [ ] 目的地资料卡已经填写并进入人工覆盖表。
- [ ] 510 个词汇、320 条短句、246 条例句齐全。
- [ ] 八个场景各有 30 条扩展短句。
- [ ] 中文和目标语言逐条语义一致。
- [ ] 中外文短句及例句均无完全重复。
- [ ] 没有机械插槽句或国家特色词汇串错包。
- [ ] 当地货币、城市、交通、住宿、食物和急救号码正确。
- [ ] 高风险内容已人工审校。
- [ ] 读音字段完整，抽查读音与目标文本一致。
- [ ] 文本冻结后才开始批量生成音频。
- [ ] 所有受影响旧音频已按精确清单删除并重建。
- [ ] 音频无缺失、无空文件、无 `.part` 残留。
- [ ] 数字、地名、缩写和紧急表达已经试听。
- [ ] 完整自动测试全部通过。
- [ ] 新目的地已经接入按需加载和语言包隔离逻辑。
- [ ] 零基础认读课程只训练文字到声音，没有混入词义或场景功能教学。
- [ ] 认读课程按语言难点定课数，并统一组织为 5 个阶段。
- [ ] 认读测试为 12 题，结课挑战为 4 个单词加 2 个短句。
- [ ] 认读课程总课数、末课和进度条均由课程数据动态计算。
- [ ] 认读进度包含当前 `curriculumVersion`，旧版迁移行为已验证。
- [ ] 认读文本和音频清单冻结后，才统一生成缺失教学音。

## 10. 当前项目中的参考文件

- `scripts/build_content_packs.js`：日语和美国英语构建入口。
- `scripts/english_phrases.js`、`scripts/japanese_phrases.js`：人工审核的扩展短句。
- `scripts/generate_korean_pack.py`：韩国韩语生成及人工覆盖。
- `scripts/generate_spanish_pack.js`、`scripts/spanish_phrases.js`、`scripts/spanish_examples.js`：西班牙西语生成及人工内容。
- `scripts/generate_russian_pack.py`：俄罗斯俄语生成及人工覆盖。
- `scripts/generate_audio.py`：正式内容、例句及认读课程音频工具。
- `scripts/音频生成说明.md`：音频命令和目录说明。
- `tests/content-quality.test.js`：跨语言内容质量门槛。
- `tests/beginner-curriculum.test.js`：五语言认读课程结构、边界和进度版本契约。

新增语言时，应复用结构和质量门槛，而不是复制某个国家的具体词汇、地名或表达。
