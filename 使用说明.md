# 语见世界 · 使用说明

语见世界是一款面向旅行者的日语、英语、韩语学习 PWA。它不要求用户学习完整的一门语言，只帮助用户掌握这趟旅行真正用得上的内容。

项目为纯静态网页，不需要服务器、数据库或外部 UI 框架，可以直接部署到 GitHub Pages 等静态托管服务。

## 启动方式

直接打开 `index.html` 可以使用主要学习功能。若要测试安装和离线能力，请通过本地 HTTP 服务打开：

```powershell
python -m http.server 8080
```

然后访问 `http://localhost:8080`。

## 主要功能

- 首页：从“打算去哪？”选择目的地与语言、继续上次学习，或自由进入机场、交通、酒店、餐厅、购物、问路、紧急求助和基础交流。日本（日语）、美国（英语）与韩国（韩语）当前可用；俄罗斯和西班牙为即将开放的预览入口。
- 旅行认读：选择已开放目的地后询问当前水平。日本提供假名与拼读规则课程；美国提供英语声音与拼读课程；韩国提供 5 个阶段、12 节从韩文音节方块到旅行标识的短课。三种语言均有认读测试和挑战，课程与八大旅行场景互不锁定。
- 场景详情：查看场景掌握进度，并直接进入任意小情境，不设关卡或解锁顺序。
- 学习：日本日语、美国英语和韩国韩语各有 795 条独立内容；每组最多认识 5 条，随后通过“中文选外语”小测确认掌握。带例句的重点词汇可展开查看上下文，例句不单独计入进度。
- 错项重学：小测答错后会重新展示对应内容，并只重测错项。
- 复习：从已掌握内容中进行双向混合测试，也可以只加强薄弱内容或按场景复习。
- 工具：工具页首屏展示中文到当前目的地语言的快捷翻译界面，并标注“待开放”；可输入和清除文字，翻译 API 尚未接入，因此暂不能生成译文。“更多旅行工具”中可制作日语、英语或韩语双语紧急联系卡；汇率换算和旅行清单仍为未来能力预览。

### 紧急联系卡

从“工具 → 紧急联系卡”填写本人、紧急联系人和可选医疗资料，即可生成横向银行卡比例的 PNG。卡片使用当前学习语言，固定国籍、过敏和疾病内容由离线词典生成双语文本；自定义内容需要同时填写中文和目标语言。

填写内容只保留在当前页面内存，不会上传，也不会写入浏览器存储。刷新或关闭页面后资料会清除。导出的图片为 1011 × 638 px（300 PPI）；打印时请选择“实际大小/100%”，成品约为 85.60 × 53.98 mm。
- 我的：查看当前目的地语言的学习数据、安装 App 或清除全部学习数据。目的地与语言统一从首页顶部切换。

## 学习状态

每条内容有三个阶段：

1. 未开始：尚未进入认识流程。
2. 已认识：看过内容，但还没有在小测中答对。
3. 已掌握：已经通过小测，可以进入复习。

复习答错不会撤销掌握状态，只会将内容加入“需要加强”；再次答对后会自动移除。

每个目的地语言包的认识、掌握、弱项及最后学习位置分别保存，互不影响。

## 本地数据

学习数据保存在浏览器的 `localStorage` 中：

- `yujianWorld.lang.v1`：当前学习语言；首次打开且尚未选择时不会写入该键。
- `yujianWorld.destination.v1`：当前目的地；旧版日语和英语选择会自动映射为日本和美国。
- `yujianWorld.learning.v2`：按 `contentPackId` 保存各目的地语言包的学习状态、弱项与最后学习位置。
- `yujianWorld.migrated.v2`：语言包版数据迁移标记。
- `yujianWorld.beginner.v1`：按目的地保存水平选择，认读课程进度保存在对应语言包下。

从旧版首次升级时，日语进度会迁入 `jp-ja`，英语进度会迁入 `us-en`；认识、掌握、弱项、复习次数和最后位置均保留。“我的 → 清除全部学习数据”不会改变当前目的地选择。

## PWA 与离线

通过 HTTPS 或 `localhost` 打开后，可以将“语见世界”安装到桌面或主屏幕。Service Worker 只预缓存首页、样式、通用脚本等小型应用外壳；联网时优先读取服务器新版本，断网时可显示已缓存的外壳。语言包和音频不进入离线预缓存，完整学习功能需要网络连接。音频按播放请求项目内对应 MP3，失败后尝试系统语音；浏览器仍可按服务器的 HTTP 缓存规则复用已下载的音频。

首次打开只加载通用代码。选择目的地后加载该目的地的内容包、认读课程与教学音频清单；同一页面内再次切回已加载的目的地不会重复请求脚本。图片和语言包不由 Service Worker 预取。

## 文件说明

- `index.html`：应用外壳、页面元信息和底部导航。
- `styles.css`：全局视觉、响应式布局和无障碍状态。
- `core/`：`destinations.js` 集中管理目的地、语言资源路径和主题图；另有语言包注册表、初学者模块注册表、紧急联系卡和收藏夹等跨语言通用模块。新增已开放语言时，添加语言目录及所需素材，再在 `destinations.js` 注册资源。
- `languages/{packId}/pack.js`：独立语言包；目前为 `jp-ja`、`us-en`、`kr-ko`，每包 795 条正式内容及 230 条场景化例句。其中 70 条按使用价值筛选后补充，内容源见 `scripts/targeted_examples.json`。
- `languages/{packId}/beginner/`：该语言自己的课程数据、教学音清单和模块实现；新增语言时可按同一目录结构扩展。
- `scripts/source-data/legacy-bilingual-data.js`：旧 555 条日英双语源数据，仅供构建脚本迁移和重新生成语言包，网页不直接加载。
- `languages/jp-ja/beginner/audio.json`：日语教学音频生成清单；使用 `python -X utf8 scripts/generate_audio.py --beginner-test` 试听，再用 `--beginner-full` 生成全部教学音。
- `app.js`：路由、学习流程、复习、存储、发音和安装逻辑。
- `manifest.webmanifest` / `sw.js`：PWA 与小型共享应用外壳缓存。
- `audio/packs/{packId}/entries/` 与 `examples/`：正式内容和例句的静态 MP3；缺失时回退到语言包配置的系统语音。
- `scripts/build_content_packs.js`：从迁移基础数据重新构建日英静态语言包；`scripts/generate_korean_pack.py` 使用已缓存并人工修订的内容重建韩语包。
- `legacy/`：历史版本备份，本次升级不修改。

## 自动化测试

```powershell
node --test tests/*.test.js
```

测试覆盖词库结构、路由、语言隔离、学习批次、掌握规则、弱项复习、PWA 配置和按需加载配置。
