# 旅行单词 · 使用说明

这是一个适合手机使用的旅行词汇学习 PWA，支持日语和英语分类学习、反向自测与错题复习。项目是纯静态网页，不需要服务器、数据库或第三方依赖，可直接发布到 GitHub Pages。

## 在线使用

1. 将 `index.html`、`styles.css`、`app.js` 和 `data.js` 放在 GitHub 仓库的同一目录。
2. 在仓库的 **Settings → Pages** 中选择发布分支和目录。
3. 等待 GitHub Pages 生成访问地址后，用手机或电脑浏览器打开。

页面使用相对资源路径，因此同时兼容用户主页和 `用户名.github.io/仓库名/` 形式的项目页面。

## 安装到桌面与离线使用

通过 GitHub Pages 或其他 HTTPS 地址打开后，支持 PWA 的浏览器可以将“旅行单词”安装到桌面或主屏幕，并以独立窗口运行。桌面端本地测试时也可以使用 `http://localhost`；直接双击打开的 `file://` 页面仍可作为普通网页使用，但无法注册 PWA。

在 Chrome、Edge 等支持网页安装事件的浏览器中，可以前往“我的 → 安装 App”调用浏览器原生安装界面。在 iPhone 或 iPad 浏览器中，该页面会提示通过“分享 → 添加到主屏幕”完成安装。

首次安装和缓存需要联网。成功打开一次后，页面、样式、程序、词库和应用图标可以离线加载，已有的学习记录仍保存在浏览器中。

发音 MP3 不由 PWA 缓存，断网时未必能播放。音频请求仍使用普通浏览器网络行为，并保留网页原有的系统语音回退；即使发音不可用，答题流程也不会被阻塞。

发布新版本时，如果修改了 `index.html`、`styles.css`、`app.js`、`data.js`、Manifest 或图标，请同步递增 `sw.js` 中的应用壳缓存版本。新 Service Worker 会在后台准备缓存，并在旧页面全部关闭后启用，不会强制刷新正在进行的练习。

## 学习方法

1. 首次打开时选择日语或英语，之后可以在“我的”中切换。两种语言的进度分别保存。
2. 在首页选择旅行场景，也可以直接开始随机练习。
3. 每轮最多 10 题。系统优先选择未学习词，其次选择正确率较低的词。
4. 选择答案并点击一次“确认答案”，页面会立即锁定选项、显示正确答案并播放对应语言的发音。
5. 发音结束后页面会短暂停留并自动进入下一题；最后一题结束后自动显示本轮结果，无需再次操作。
6. “自测”使用外文题目和中文选项，只从当前语言已学习的词条中抽题。
7. 答错的词会进入当前语言的错题本；在自测或错题复习中答对后会自动移除。

发音优先使用项目内对应语言的 MP3，播放失败时回退到浏览器和操作系统提供的语音合成功能。没有对应语音、播放失败或超时时，反馈仍会保留片刻并自动继续，不会卡住答题流程。

## 学习记录

学习进度、答题次数、正确率、错题和语言选择保存在浏览器的 `localStorage` 中：

- 关闭或刷新网页不会丢失记录。
- 不同浏览器、设备和站点域名之间不会自动同步。
- 更改仓库名、GitHub Pages 地址或自定义域名后，会产生一份独立记录。
- 日语和英语分别记录学习进度与错题。
- “我的 → 清除全部学习数据”会清除两种语言的学习记录和错题，但保留语言选择。
- 从旧版升级时，无法区分语言的旧学习记录和错题会被清除一次。

## 词库格式

词库位于 `data.js`。每条原始记录格式如下：

```js
["稳定ID", "中文", "日文", "假名", "英文", "word或phrase"]
```

新增词条时：

- 使用从未出现过的稳定 ID，例如 `hotel_083`。
- 不要因为排序或移动词条而修改已有 ID，否则旧学习记录会失去对应关系。
- 分类必须存在于 `window.CATEGORIES` 中。
- 同一分类和类型中，应避免出现显示内容完全相同的答案。

## 本地预览

可以直接双击 `index.html`，也可以在项目目录启动任意静态 HTTP 服务器。语音和本地存储行为以 GitHub Pages 的 HTTPS 页面为准。

推荐使用最新版 Chrome、Edge、Safari 或 Firefox。
